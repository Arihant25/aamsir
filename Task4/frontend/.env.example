# ── Backend API ───────────────────────────────────────────────────────────────
# Full URL (including /api prefix) of the AAMSIR backend.
# Must be reachable from the browser, so use the machine's LAN IP when the
# frontend and backend run on different hosts.
NEXT_PUBLIC_API_URL=http://localhost:8000/api
